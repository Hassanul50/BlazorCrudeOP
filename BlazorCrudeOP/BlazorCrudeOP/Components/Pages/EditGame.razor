@page "/edit-game"
@page "/edit-game/{id :int}"
<h3>Create Game</h3>
<EditForm Model="game" OnSubmit="HandelSubmit">
    <label>Name</label>
    <InputText @bind-Name="game.GameName">@game.GameName</InputText>
    <button type="submit" class="btn btn-success">Submit</button>

</EditForm>

@code {
    Game game = new Game { GameName = String.Empty };
    async Task HandelSubmit()
    {
        //
    }
}
